# CodeLens dependencies. No GPU, no external API keys, no model weight
# downloads required, everything here runs fully offline.
fastapi>=0.110
uvicorn[standard]>=0.29
scikit-learn>=1.4
numpy>=1.26
pydantic>=2.6

pytest>=8.0
httpx>=0.27
